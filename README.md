# File Lister

List files in current directory. I work on different OS's and getting mixed up between `ls` and `dir` is annoying, and never need 90% of the information that `dir` puts out, so I just created this.

## Build & Run

Build: `jai list.jai`

Run: `list`
