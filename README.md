# File Lister

List files in current directory. I switch between MacOS and Windows all the time, and get `ls` and `dir` mixed up. I also don't need like 90% of the information that `dir` gives me, so just created this.

## Build & Run

Build: `jai list.jai`

Run: `list`
